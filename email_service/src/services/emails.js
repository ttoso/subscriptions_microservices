
const EmailService = {

    sendEmail: async (message) => {
        // Here should be implemented the email sending logic
        console.log('Email sent to ' + message);
    }
};

module.exports = EmailService;